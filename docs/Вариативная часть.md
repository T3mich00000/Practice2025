# 2.Вариативная часть задания 
Выбор тематики. Мной была выбрана тема – «Реализация эффекта Параллакса на JavaScript».
# 📝 Проект: Реализация эффекта Параллакса на JavaScript

## 📌 Цель проекта
Разработка универсального скрипта для создания эффекта параллакса на веб-страницах с использованием чистого JavaScript, обеспечивающего плавное перемещение элементов при скролле с учетом доступности и производительности.

## 📌 Задачи проекта
-Изучить принципы работы эффекта параллакса в веб-разработке.
-Разработать скрипт, автоматически определяющий элементы с эффектом параллакса.
-Реализовать настройку скорости, направления и масштабирования эффекта.
-Обеспечить поддержку пользовательских настроек уменьшения движения для доступности.
-Оптимизировать производительность с помощью will-change и requestAnimationFrame.
-Реализовать обработку событий скролла и изменения размера окна.
-Провести тестирование скрипта на различных устройствах и браузерах.

## 🔍 Исследование и разработка

### 1. Выбор тематики

Была выбрана тема: **«Реализация эффекта Параллакса на JavaScript»** — современный подход к созданию интерактивных веб-интерфейсов с улучшенным визуальным восприятием.
### 2. Стек технологий

| Компонент       | Выбор                   |
|-----------------|-------------------------|
| Язык            | JavaScript (ES6+)       |
| API браузера    | DOM, CSS Transforms     |

---

### 3. Изучение технологии
Мной был изучен принцип работы параллакс-эффекта и современные подходы к его реализации:

-Создан основной скрипт: Animation.js, в котором реализованы:
-Автоматическое обнаружение элементов с атрибутом data-parallax
-Поддержка различных осей перемещения (X, Y)
-Настройка скорости эффекта через data-атрибуты
-Проверка пользовательских предпочтений уменьшения движения
-Оптимизация производительности через will-change и transform

---

## 🛠 Техническое руководство

### Необходимое ПО

-Веб-браузер с поддержкой ES6+ (Chrome, Firefox, Safari, Edge)
-Текстовый редактор для написания кода

---

### 🧩 Часть 1: Инициализация и проверка доступности и основная функция настройки параллакса

```JavaScript
(() => {
  const REDUCED = window.matchMedia('(prefers-reduced-motion: reduce)').matches;

  function setupParallax() {
    const items = document.querySelectorAll('[data-parallax]');
    if (!items.length || REDUCED) return;

```
### 🧩 Часть 2: Подготовка элементов
```JavaScript
items.forEach(el => {
      el.style.willChange = 'transform';
      const s = parseFloat(el.dataset.parallaxScale || '1');
      if (!Number.isNaN(s) && s !== 1) el.dataset._scale = String(s);
    });
```

### 🧩 Часть 3: Система обновления с throttling
```JavaScript
let ticking = false;

const onScrollOrResize = () => {
  if (!ticking) {
    requestAnimationFrame(update);
    ticking = true;
  }
};
```
### 🧩 Часть 4: Расчет параллакс-эффекта
```JavaScript
const update = () => {
      ticking = false;
      const vh = window.innerHeight;

      items.forEach(el => {
        const speed = Number(el.dataset.parallax || 0.2) || 0;
        const axis = (el.dataset.parallaxAxis || 'y').toLowerCase();
        const rect = el.getBoundingClientRect();
        const center = rect.top + rect.height / 2 - vh / 2;
        const offset = Math.round(center * speed);
        const dx = axis === 'x' ? offset : 0;
        const dy = axis === 'y' ? offset : 0;
        const scale = parseFloat(el.dataset._scale || '1');
        const scaleStr = scale !== 1 ? ` scale(${scale})` : '';
        el.style.transform = `translate3d(${dx}px, ${dy}px, 0)${scaleStr}`;
      });
    };
```

### 🧩 Часть 5: Подписка на события
```JavaScript
    window.addEventListener('scroll', onScrollOrResize, { passive: true });
    window.addEventListener('resize', onScrollOrResize);
    update();
```
### 🧩 Часть 6: Запуск скрипта
```JavaScript
  document.addEventListener('DOMContentLoaded', setupParallax);
})();
```

### ✍ Подведение итогов и вывод
В рамках данного проекта была успешно реализована цель — создание универсального скрипта для эффекта параллакса, который автоматически определяет элементы с соответствующими data-атрибутами и применяет к ним плавное перемещение при скролле. Скрипт учитывает современные требования к доступности, проверяя пользовательские предпочтения уменьшения движения, и оптимизирован для производительности через использование will-change и requestAnimationFrame.

Вывод: Проект позволил глубоко изучить принципы работы с DOM, CSS-трансформациями и оптимизацией анимаций в браузере. Разработанное решение является легковесным, не требует внешних зависимостей и может быть легко интегрировано в любой веб-проект. Скрипт предоставляет гибкие настройки через data-атрибуты, позволяя тонко настраивать поведение эффекта для каждого элемента. Таким образом, поставленные задачи выполнены в полном объеме.

### 📄 Список литературы
1. MDN Web Docs. CSS will-change [Электронный ресурс]. – Режим доступа: https://developer.mozilla.org/ru/docs/Web/CSS/will-change (дата обращения: 20.05.2025).

2. MDN Web Docs. prefers-reduced-motion [Электронный ресурс]. – Режим доступа: https://developer.mozilla.org/ru/docs/Web/CSS/@media/prefers-reduced-motion (дата обращения: 20.05.2025).

3. JavaScript Parallax Effect Tutorial [Электронный ресурс] // FreeCodeCamp. – Режим доступа: https://www.freecodecamp.org/news/parallax-effect-javascript/ (дата обращения: 20.05.2025).

### 📺 Ссылка на видео презентацию
